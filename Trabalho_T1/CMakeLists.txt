# Versão mínima do CMake
cmake_minimum_required(VERSION 3.16)

# Inclui a funcionalidade do sistema de build do ESP-IDF
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Define o nome do projeto
project(Trabalho_T1)